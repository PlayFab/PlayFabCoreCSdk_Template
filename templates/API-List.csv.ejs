<%


%>SDKFeatureGroups:
for (var featureGroupName in SDKFeatureGroups) {
    %><%- featureGroupName + "," %> 
    <%
}

Area,Call,Example API, Classic Client, Classic Server, Classic Admin, Entity Group, SubGroup
<%

for (var featureGroupName in SDKFeatureGroups) {
    %><%- featureGroupName + "," %> 
    <%
}

%>